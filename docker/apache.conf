<VirtualHost *:80>
    ServerName localhost
    DocumentRoot ${APACHE_DOCUMENT_ROOT}

    <Directory ${APACHE_DOCUMENT_ROOT}>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
